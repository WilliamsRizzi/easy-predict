image: python:3.12

stages:
  - test

variables:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"

before_script:
  - python -m pip install --upgrade pip
  - python -m pip install -e .

test:
  stage: test
  script:
    - pytest -q tests
